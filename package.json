{
  "name": "qing-queue-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check dist/server/index.js",
    "build": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
